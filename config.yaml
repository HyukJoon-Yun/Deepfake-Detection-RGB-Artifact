lr: 0.0005
batch_size: 4
num_epochs: 10

train_paths:
  - "data/train"

val_paths:
  - "data/validation"

input_mode: "artifact"
checkpoint_filename: "artifact_map_model"

early_stopping_patience: 4
monitor_metric: "val_loss"
monitor_mode: "min"
log_every_n_steps: 1
logger: false